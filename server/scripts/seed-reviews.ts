/**
 * Demo seed for reviews is disabled — reviews are user-submitted only.
 * Use `npm run clear:reviews` to wipe existing review rows.
 */
console.log('No-op: customer reviews are dynamic (user-submitted). Nothing to seed.');
