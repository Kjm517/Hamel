# Legacy Supabase SQL — superseded by Neon schema in ../server/sql/001_schema.sql
# Keep these files only as historical reference. Do not run them against Neon as-is
# (they use Supabase Auth RLS, storage.buckets, and auth.uid()).
