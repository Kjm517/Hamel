# Optional: leave empty to use Vite proxy (/api → localhost:8787)
# For Vercel production, set to your public API origin (no trailing slash), e.g.:
# VITE_API_URL=https://hamel-api.onrender.com

# Set to "false" to disable default tag-icon path fallbacks
# VITE_TAG_ICON_FALLBACK=true
